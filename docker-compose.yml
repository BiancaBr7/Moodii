services:
  backend:
    build:
      context: ./backend/moodii
      dockerfile: ../../docker/backend/Dockerfile
    ports:
      - "8080:8080"
    # Removed volume mount as discussed earlier

  ml:
    build: ./docker/ml
    ports:
      - "8000:8000"
    command: python -m http.server 8000
