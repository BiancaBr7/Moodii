# Application properties for Moodii
# This file contains configuration settings for the Moodii application
# Project Name: Moodii
spring.application.name=moodii

# Database configuration - MongoDB Atlas
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://biancabr7:cywkurpjeMIc2vyC@moodii-cluster.qbiqko9.mongodb.net/moodii?retryWrites=true&w=majority&appName=moodii-cluster}

# JWT Secret and expiration
moodii.jwt.secret=secretKey
moodii.jwt.expiration=3600000

# Server configuration
# The server will run on port 8080
# You can change this port if needed
server.port=8080
server.address=0.0.0.0

# ML service base URL (override with ML_API_URL env when deploying)
ml.service.url=${ML_API_URL:http://localhost:5000}


spring.data.mongodb.auto-index-creation=true

# Logging
logging.level.com.moodii=INFO
logging.file.name=/app/logs/moodii-backend.log

# File upload configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Upload directory
moodii.upload.dir=/app/uploads

# Health check endpoint
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always